streamlit
openai==1.59.6
# unpinned, unlike the Docker image (Python 3.11) - Streamlit Cloud's
# runtime is whatever Python they currently default to (3.14 as of this
# writing), and torch==2.4.1/transformers==4.44.2 predate that entirely.
# This deployment doesn't need version parity with the submission image.
#
# No llama-cpp-python here - this deployment doesn't run the local Qwen
# model at all (too much memory/disk for this tier, and it kept
# segfaulting the process). "local"-routed queries get answered by a
# cheap Fireworks model instead (see app.py's get_cheap_model()).
transformers
--extra-index-url https://download.pytorch.org/whl/cpu
torch
