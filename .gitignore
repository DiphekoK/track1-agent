.env
__pycache__/
*.pyc
models/*.gguf
output/results.json

# data/queries.jsonl, data/labeled_dataset.jsonl and router/model/ used to
# live here too - reproducible from the scripts in theory, but the whole
# point of committing them now is that the Dockerfile's COPY needs the
# trained weights to actually be sitting in the repo, not regenerated on
# every build (see .github/workflows/train-router.yml)
router/checkpoints/
